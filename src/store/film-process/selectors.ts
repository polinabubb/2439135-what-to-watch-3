/*import { State } from '../../types/state';
import { Genre, NameSpace } from '../../const.ts';

export const getGenre = (state: State): Genre => state[NameSpace.Film].genre;
export const getFilmsCount = (state: State): number =>
  state[NameSpace.Film].genreFilmsCount;
export const getSimilarFilmsCount = (state: State): number =>
  state[NameSpace.Film].similarFilmsCount;
*/
